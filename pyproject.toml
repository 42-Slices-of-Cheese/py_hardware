[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "py-hardware"
version = "0.1.0"
description = "A hardware simulation framework for Python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
dynamic = ["version"]

[tool]
